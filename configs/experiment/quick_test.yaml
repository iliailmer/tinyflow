# @package _global_

# Quick test configuration for development

defaults:
  - override /model: neural_network
  - override /scheduler: linear
  - override /optimizer: adam
  - override /dataset: moons
  - override /training: moons_default

optimizer:
  lr: 0.01

training:
  num_epochs: 100
  log_interval: 10
  num_samples: 50
  num_plots: 5

dataset:
  n_samples: 50
  noise: 0.05

mlflow:
  experiment_name: "quick_tests"
  log_models: false
